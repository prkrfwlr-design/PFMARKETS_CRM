# ⬇️ Drag 5 files here from the USB to finish the CRM copy

Everything else in the CRM is already here (data, schema, config, setup guide, and the
old v1 `crm.html`). Only the 5 large app files still need to be copied — they're big
live web apps, so a direct drag-and-drop from the USB is the safe, exact way to move them.

On the **Digital Marketing USB**, open the `CRM` folder and copy these:

| File on USB | Drag it into |
|---|---|
| `CRM/agency.html` | `02-CRM-System/app/`  ← this folder |
| `CRM/portal.html` | `02-CRM-System/app/`  ← this folder |
| `CRM/cloud/agency.html` | `02-CRM-System/app/cloud/` |
| `CRM/cloud/index.html` | `02-CRM-System/app/cloud/` |
| `CRM/cloud/employees.html` | `02-CRM-System/app/cloud/` |

Once they're in, delete this note. The USB keeps the originals as your backup.
